# Service Fragments Placeholder

This folder is reserved for modular service snippets, matching the
pentest_lab pattern where service YAMLs are split and later merged.

For this step, `docker/docker-compose.yml` is the clean merged output.
